---
import IconX from "@/assets/icons/IconX.svg";
import IconMoon from "@/assets/icons/IconMoon.svg";
import IconSunHigh from "@/assets/icons/IconSunHigh.svg";
import IconMenuDeep from "@/assets/icons/IconMenuDeep.svg";
import IconSearch from "@/assets/icons/IconSearch.svg";
import IconEdit from "@/assets/icons/IconEdit.svg";
import IconArchive from "@/assets/icons/IconArchive.svg";
import IconHash from "@/assets/icons/IconHash.svg";
import LinkButton from "./LinkButton.astro";
import { SITE } from "@/config";

const { pathname } = Astro.url;

// Remove trailing slash from current pathname if exists
const currentPath =
  pathname.endsWith("/") && pathname !== "/" ? pathname.slice(0, -1) : pathname;

const isActive = (path: string) => {
  const currentPathArray = currentPath.split("/").filter(p => p.trim());
  const pathArray = path.split("/").filter(p => p.trim());

  return currentPath === path || currentPathArray[0] === pathArray[0];
};
---

<a
  id="skip-to-content"
  href="#main-content"
  class="absolute start-16 -top-full z-50 bg-background px-3 py-2 text-accent backdrop-blur-lg transition-all focus:top-4"
>
  Skip to content
</a>

<header
  class="flex w-full flex-col items-center justify-between bg-accent text-white sm:flex-row"
>
  <div
    id="top-nav-wrap"
    class:list={[
      "app-layout",
      "py-4 sm:py-6",
      "relative w-full",
      "flex items-baseline justify-between sm:items-center",
    ]}
  >
    <a href="/" class="absolute flex items-center py-1 sm:static sm:my-auto">
      <img
        src="https://kasnerz.github.io/static/img/icon.png"
        alt="Zdeněk Kasner Logo"
        class="h-12 w-12 rounded-full sm:h-16 sm:w-16"
      />
    </a>
    <nav
      id="nav-menu"
      class="flex w-full flex-col items-center sm:ms-2 sm:flex-row sm:justify-end sm:space-x-4 sm:py-0"
    >
      <button
        id="menu-btn"
        class="focus-outline self-end p-2 sm:hidden"
        aria-label="Open Menu"
        aria-expanded="false"
        aria-controls="menu-items"
      >
        <IconX id="close-icon" class="hidden" />
        <IconMenuDeep id="menu-icon" />
      </button>
      <ul
        id="menu-items"
        class:list={[
          "mt-4 grid w-44 grid-cols-2 place-content-center gap-2",
          "hidden",
          "sm:mt-0 sm:flex sm:w-auto sm:gap-x-5 sm:gap-y-0",
        ]}
      >
        <li class="col-span-2">
          <a
            href="/research"
            class:list={[
              { "active-nav": isActive("/research") },
              "focus-outline flex flex-col items-center justify-center gap-1 px-4 py-3 hover:text-gray-200 sm:px-2 sm:py-1",
            ]}
          >
            <div class="size-6 sm:size-6">
              <IconSearch class="h-full w-full" />
            </div>
            <span class="font-medium">Research</span>
          </a>
        </li>
        <li class="col-span-2">
          <a
            href="/teaching"
            class:list={[
              { "active-nav": isActive("/teaching") },
              "focus-outline flex flex-col items-center justify-center gap-1 px-4 py-3 hover:text-gray-200 sm:px-2 sm:py-1",
            ]}
          >
            <div class="size-6 sm:size-6">
              <IconEdit class="h-full w-full" />
            </div>
            <span class="font-medium">Teaching</span>
          </a>
        </li>
        <li class="col-span-2">
          <a
            href="/projects"
            class:list={[
              { "active-nav": isActive("/projects") },
              "focus-outline flex flex-col items-center justify-center gap-1 px-4 py-3 hover:text-gray-200 sm:px-2 sm:py-1",
            ]}
          >
            <div class="size-6 sm:size-6">
              <IconArchive class="h-full w-full" />
            </div>
            <span class="font-medium">Projects</span>
          </a>
        </li>
        <li class="col-span-2">
          <a
            href="/about"
            class:list={[
              { "active-nav": isActive("/about") },
              "focus-outline flex flex-col items-center justify-center gap-1 px-4 py-3 hover:text-gray-200 sm:px-2 sm:py-1",
            ]}
          >
            <div class="size-6 sm:size-6">
              <IconHash class="h-full w-full" />
            </div>
            <span class="font-medium">About</span>
          </a>
        </li>
        {
          SITE.lightAndDarkMode && (
            <li class="col-span-1 flex items-center justify-center">
              <button
                id="theme-btn"
                class="focus-outline flex flex-col items-center justify-center gap-1 px-2 py-1 hover:text-gray-200"
                title="Toggles light & dark"
                aria-label="auto"
                aria-live="polite"
              >
                <div class="relative size-6 sm:size-6">
                  <IconMoon class="absolute top-0 left-0 scale-100 rotate-0 transition-all dark:scale-0 dark:-rotate-90" />
                  <IconSunHigh class="absolute top-0 left-0 scale-0 rotate-90 transition-all dark:scale-100 dark:rotate-0" />
                </div>
                <span class="font-medium">Light/Dark</span>
              </button>
            </li>
          )
        }
      </ul>
    </nav>
  </div>
</header>

<script>
  function toggleNav() {
    const menuBtn = document.querySelector("#menu-btn");
    const menuItems = document.querySelector("#menu-items");
    const menuIcon = document.querySelector("#menu-icon");
    const closeIcon = document.querySelector("#close-icon");

    if (!menuBtn || !menuItems || !menuIcon || !closeIcon) return;

    menuBtn.addEventListener("click", () => {
      const openMenu = menuBtn.getAttribute("aria-expanded") === "true";

      menuBtn.setAttribute("aria-expanded", openMenu ? "false" : "true");
      menuBtn.setAttribute("aria-label", openMenu ? "Open Menu" : "Close Menu");

      menuItems.classList.toggle("hidden");
      menuIcon.classList.toggle("hidden");
      closeIcon.classList.toggle("hidden");
    });
  }

  toggleNav();

  // Runs on view transitions navigation
  document.addEventListener("astro:after-swap", toggleNav);
</script>
